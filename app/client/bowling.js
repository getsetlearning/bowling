angular.module('interview.service.bowling', [ ]).

factory('bowling', [
  function() {
    return function service() {
      var game = { };

      game.roll = function(pins) {

      };

      game.score = function() {

      };

      return game;
    };
  }
]);

